import { PaginatorPagesPipe } from './paginator-pages.pipe';

describe('PaginatorPagesPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginatorPagesPipe();
    expect(pipe).toBeTruthy();
  });
});
